<div{{ attributes }}>
    <div class="container mx-auto px-16">
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.emails) }}
        {{ form_end(form) }}
    </div>
</div>
